#!/usr/bin/env node
var big = require('../big');

big.use('http');
big.use('replicator');

big.start(function(err, server){
  if (err) {
    throw err;
  }
  big.listen();
  console.log('replicator service listening on port ' + server.address().port);
});