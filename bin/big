#!/usr/bin/env node

var resource = require('resource'),
    resources = require('resources'),
    logger = resource.logger;

logger.help('welcome to big');

resource.cli.start(function (err, router) {
  router.route();
});
