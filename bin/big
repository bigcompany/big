#!/usr/bin/env node

var big = require('../big'),
    resources = require('resources'),
    logger = big.logger,
    cli = big.use('cli');

logger.help('welcome to big');

cli.start(function(err, router){
  router.route();
});
